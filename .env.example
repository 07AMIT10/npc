# Smart NPC Explorer - Configuration
# Copy this file to .env and add your API keys
# cp .env.example .env

# ========== Server ==========
PORT=8080

# ========== Per-NPC Provider & Model Assignment ==========
# Format: NPC_<name>_PROVIDER=<provider_name>
# Format: NPC_<name>_MODEL=<model_name>  (optional, overrides provider default)
#
# Available providers: groq, sambanova, openrouter, huggingface, nebius
# If not set, NPCs will round-robin across all available providers

NPC_EXPLORER_PROVIDER=groq
NPC_SCOUT_PROVIDER=sambanova
NPC_WANDERER_PROVIDER=huggingface
NPC_SEEKER_PROVIDER=nebius

# ========== SLM Providers (Fast Action Decisions) ==========

# Groq (recommended - very fast, free tier)
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# SambaNova (fast, generous free tier)
SAMBANOVA_API_KEY=
SAMBANOVA_MODEL=Meta-Llama-3.1-8B-Instruct

# OpenRouter (many models, pay per token)
OPENROUTER_API_KEY=
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct

# HuggingFace Router (many free models)
# Working models: meta-llama/Llama-3.2-3B-Instruct, Qwen/Qwen2.5-7B-Instruct
HF_API_KEY=
HF_MODEL=meta-llama/Llama-3.2-3B-Instruct

# Nebius Token Factory (fast, free tier)
NEBIUS_API_KEY=
NEBIUS_MODEL=meta-llama/Meta-Llama-3.1-8B-Instruct

# ========== Brain LLM (Strategic Thinking) ==========

# Google Gemini (recommended, generous free tier)
# Models: gemini-2.0-flash, gemini-1.5-flash, gemini-1.5-pro
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
